server:
  port: 8080

spring:
  application:
    name: customer
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/CustomerDb
    username: postgres
    driver: org.postgresql.Driver
